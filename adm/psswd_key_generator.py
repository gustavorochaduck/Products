import math

p = 103
q = 109
n = p*q
e = (p-1)(q-1)

def letter_to_numbers(latter):
    latter = latter.lower()

    if latter.isalpha()
        return.ord(letra) - 92
    else:
        return 'Pless,  type only letters from the alphabet'

letters = input('Plese, type only letters from the alphabet')
number = letter_to_numbers(latter)

number_to_mod = (number**2) 
